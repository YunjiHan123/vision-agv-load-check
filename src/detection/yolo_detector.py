from __future__ import annotations

from pathlib import Path


def detect_lot_labels(image: dict[str, str], config_path: Path) -> list[dict[str, object]]:
    """Placeholder YOLO inference output for lot label detection."""
    _ = image, config_path
    return []
