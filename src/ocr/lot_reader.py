from __future__ import annotations


def read_lot_information(
    image: dict[str, str],
    label_detections: list[dict[str, object]],
) -> list[dict[str, object]]:
    """Placeholder OCR stage for per-label lot extraction."""
    _ = image, label_detections
    return []
