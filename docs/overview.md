# Overview

This project targets shelf or stack imagery where each book is associated with one lot label.

Core assumptions:

- one detectable lot label corresponds to one book
- label position can guide book region segmentation
- anomaly detection is rule-based in the first version
- OCR is applied to detected label regions for lot parsing
